# 非推奨ファイル

このディレクトリには、使用されなくなったファイルが保管されています。

## shiftGenerator.ts.backup

**削除理由**: フロントエンドのシフト生成ロジックは使用されていません。

**現在の実装**: ShiftGenerator.tsxは、APIサーバーの`/api/generate-shifts`エンドポイントを呼び出しています。

**削除日**: 2025年12月26日

**影響範囲**: なし（既にAPIサーバーを使用しているため）

**バックアップ**: このファイルは参照用として保管されています。

**ハードコードされていた制約**:
- `MAX_DAILY_WORK_HOURS = 15` (1日の最大労働時間)
- `checkDailyWorkHours()` 関数

これらの制約は、バックエンドのルールエンジン（`api-server/RuleEngine.ts`）で管理されるようになりました。
